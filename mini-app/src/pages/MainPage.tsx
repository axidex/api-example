import type { FC } from 'react';
import { Page } from '@/components/Page.tsx';

export const MainPage: FC = () => (
    <Page>
        <div style={{textAlign: 'center', paddingTop: 40}}>Main page</div>
    </Page>
);